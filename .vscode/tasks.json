{
    "tasks": [
        {
            "type": "shell",
            "label": "compile makefile",
            "command": "/usr/bin/gcc",
            "args": [
				"make"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}